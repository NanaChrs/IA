"use strict";
var deformation = function() {

    this.load = function(element, points, vertices) {
        if (vertices) {
			verticeMap = vertices;
		} else {
			verticeMap = vertices;
		}
        numTriangles = verticeMap.length;
    }
}
